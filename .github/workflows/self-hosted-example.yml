name: Testing Self-hosted Runner 

on:
    workflow_dispatch:

jobs:
    test:
        runs-on: [self-hosted, Linux, X64]
        steps:
            - name: Echo command
              run: |
                echo OK
                sleep 15s
